class News {
  String title, description, source, thumbnailURL, uploadDate, url;
  List tags;

  News(this.title, this.description, this.source, this.thumbnailURL, this.uploadDate, this.tags, this.url);
}